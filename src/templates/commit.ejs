<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/static/css/main.css">
  <link rel="stylesheet" href="/static/css/pr.css">
  <link rel="stylesheet" href="/static/css/github-markdown-light.css">
  <style>
    .commit-container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 1rem;
    }

    .commit-header {
      background: white;
      border: 1px solid #ddd;
      border-radius: 6px;
      padding: 1.5rem;
      margin-bottom: 1.5rem;
    }

    .commit-message {
      font-size: 1.5rem;
      font-weight: 600;
      margin: 0 0 0.5rem 0;
    }

    .commit-meta {
      color: #666;
      font-size: 0.9rem;
    }

    .commit-sha {
      font-family: monospace;
      background: #f6f8fa;
      padding: 0.25rem 0.5rem;
      border-radius: 3px;
      margin-left: 0.5rem;
    }

    .files-changed {
      margin-top: 1.5rem;
    }

    .files-changed h2 {
      font-size: 1.25rem;
      margin-bottom: 1rem;
    }

    /* Responsive */
    @media (max-width: 768px) {
      .commit-container { padding: 0.75rem; }
      .commit-header { padding: 1rem; }
      .commit-message { font-size: 1.25rem; }
    }

    @media (max-width: 480px) {
      .commit-container { padding: 0.5rem; }
      .commit-header { padding: 0.75rem; }
      .commit-message { font-size: 1rem; }
      .commit-meta { font-size: 0.8rem; }
      .commit-sha { font-size: 0.8rem; margin-left: 0; display: block; margin-top: 0.25rem; }
      .files-changed h2 { font-size: 1rem; }
    }
  </style>
</head>
<body>
  <header class="site-header">
    <nav class="nav-container">
      <a href="/dashboard" class="nav-logo">Argus</a>
      <div class="nav-links">
        <a href="/dashboard">Dashboard</a>
      </div>
      <% if (user) { %>
        <a href="/notifications" class="nav-notifications" id="nav-notifications" title="Review requests">
          <svg width="16" height="16" viewBox="0 0 16 16" fill="currentColor"><path d="M8 16a2 2 0 001.985-1.75c.017-.137-.097-.25-.235-.25h-3.5c-.138 0-.252.113-.235.25A2 2 0 008 16zM3 5a5 5 0 0110 0v2.947c0 .05.015.098.042.139l1.703 2.555A1.519 1.519 0 0113.482 13H2.518a1.519 1.519 0 01-1.263-2.36l1.703-2.554A.255.255 0 003 7.947V5z"/></svg>
          <span class="notification-badge hidden" id="notification-badge"></span>
        </a>
        <span class="nav-user"><%= user.login %></span>
      <% } %>
    </nav>
  </header>

  <main class="commit-container">
    <div class="commit-header">
      <h1 class="commit-message"><%= commit.commit.message.split('\n')[0] %></h1>
      <div class="commit-meta">
        <% if (commit.commit.author && commit.commit.author.name) { %>
          <%= commit.commit.author.name %> committed
          <% if (commit.commit.author.date) { %>
            on <%= new Date(commit.commit.author.date).toLocaleString() %>
          <% } %>
        <% } %>
        <span class="commit-sha"><%= commit.sha.slice(0, 7) %></span>
      </div>
      <% if (commit.commit.message.includes('\n')) { %>
        <pre style="margin-top: 1rem; white-space: pre-wrap; font-size: 0.9rem; color: #666;"><%= commit.commit.message.split('\n').slice(1).join('\n').trim() %></pre>
      <% } %>
    </div>

    <div class="files-changed">
      <h2>Files changed (<%= files.length %>)</h2>
      <div class="diff-container">
        <% files.forEach((f, i) => { %>
          <div class="diff-file-wrapper">
            <%- f.renderedHtml %>
          </div>
        <% }) %>
      </div>
    </div>
  </main>
  <script src="/static/js/notifications.js"></script>
</body>
</html>
